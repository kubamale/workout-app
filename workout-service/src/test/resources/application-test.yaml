eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

spring:
  datasource:
    username: user
    password: password
    url: jdbc:h2:mem:test-db
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never
